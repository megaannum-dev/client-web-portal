# Dev/test-only dependencies for the api-backend test harness (B0).
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Pinned to the versions present in the shared .venv used by CI.
-r requirements.txt

pytest>=8.0,<10
httpx>=0.27,<1
ruff>=0.5
mypy>=1.10
